# v0.1 MVP 验收证据

更新时间：2026-09-08

| ID | 状态 | 实现与自动化证据 |
|---|---|---|
| MVP-01 | PASS | `LegacyProjectAnalyzer` + `LegacyAnalysisAndPlanContractTest`：固定 SHA、技术栈、模块、架构图与源码证据。 |
| MVP-02 | PASS | `ArticleMigrationPlanFactory`：7 个工作包、依赖、关键路径、资源、允许路径与验收命令。 |
| MVP-03 | PASS | `GitWorktreeManagerContractTest`：三个独立 task branch/worktree，幂等重试、规范化碰撞隔离、缺失基线元数据安全对账且 main SHA 不变。 |
| MVP-04 | PASS | `PathLeaseRegistryContractTest`：重叠冲突原因、兼容矩阵、TTL、心跳和条件释放。 |
| MVP-05 | PASS | `GovernedProjectContractTest` 与 `ControlPlaneApiContractTest`：MCP Contract 事件使依赖任务进入 `STALE`，重复事件幂等。 |
| MVP-06 | PASS | `GovernedProjectContractTest`：允许/禁止路径校验，越权提交被拒绝并记录 `ChangesetRejected`。 |
| MVP-07 | PASS | `IntegrationConflictAnalyzerContractTest`：路径、API、Schema 冲突证据、影响任务与建议顺序；禁止自动合并。 |
| MVP-08 | PASS | `ArticleApiContractTest`、`ArticleWorkspace`、OpenAPI 和启动时执行的 `V1__create_modern_articles.sql`：H2 持久化 Spring Boot CRUD、Vue 3 增删改查与非破坏性数据库方案。 |
| MVP-09 | PASS | `GovernedProjectContractTest`：缺少任一门禁时拒绝 `ACCEPTED`，审批角色不匹配被拒绝，语义失效会清除旧 Gate。 |
| MVP-10 | PASS | `ManualMergeProposal`：全部门禁通过后仍固定 `humanApprovalRequired=true`、`automaticallyMergeMain=false`。 |
| MVP-11 | PASS | Git 创建幂等、记录并安全补全原始基准且不覆盖并发胜者；`ControlPlaneApiContractTest`、`AuditEventRepositoryContractTest` 与 `McpRuntimeRecoveryContractTest` 验证数据库权威幂等、凭证脱敏、结果日志、数据库生成的执行全序、`PENDING` 恢复，以及重启后任务/语义/未过期租约重建。 |
| MVP-12 | PASS | `AuditReport` 汇总任务、事件类型、时间线与风险；模型审计覆盖模型、提示版本、哈希、工具和 token。 |

## 新鲜验证要求

每次发布或完成声明都必须重新执行：

1. 后端 `mvn verify`。
2. 前端 `npm test`、`npm run typecheck`、`npm run build`。
3. 启动后端并读取 `/actuator/health`、`/api/v1/demo/overview`、`/mcp`。
4. 启动前端，确认八角色切换、后端连接状态和文章切片无控制台错误。
5. `git diff --check`，并确认父工作区无关改动未被纳入提交。

## 生产化边界

v0.1 是单项目、单节点参考实现。H2、本地角色模拟和进程内租约不是多节点生产部署方案；生产落地必须替换为受管数据库/分布式租约、OIDC/LDAP、TLS 与集中审计。此边界不改变 MVP 的人工合并、源码不出内网和条件写入不变量。
