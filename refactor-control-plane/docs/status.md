# 当前状态

- 更新时间：2026-09-08
- 规格：v0.1 已批准并持久化
- 阶段：v0.1 MVP 完成，已通过自动化与全栈浏览器验收

## 已确认

- 核心产品边界、角色、权限和人工合并策略。
- Worktree/branch + ownership/lease 的并发隔离方案。
- Shared Context + Change Event + MCP 的语义协作方案。
- OpenAI-compatible 企业模型网关、Mock 和通用 HTTP 执行器。
- SpringMvcBlog 固定提交作为遗留项目样例。

## 当前里程碑

已建立：

- Spring Boot 3.5 / Java 21 后端构建骨架与应用入口。
- 37 个后端契约测试覆盖分析、规划、状态机、租约、Git、中断恢复、模型、冲突、MCP、按 journal sequence 重启恢复、持久化、门禁和文章 API。
- 路径租约使用服务端单调时间、过期清理、项目级大小写策略和版本条件更新。
- 固定快照的证据化遗留分析与 7 个工作包文章迁移 DAG。
- 三个独立 Git task branch/worktree 的创建、规范化碰撞隔离、中断后安全补全元数据与幂等重试验证。
- Shared Context 失效传播、越权变更拒绝、四类质量门禁、审计导出和人工合并建议。
- MCP 2025-03-26 JSON-RPC 入口及 15 个批准工具，写操作驱动租约/治理域并持久化到 H2 审计日志，通知、令牌、幂等和版本冲突有契约测试。
- Mock 与 OpenAI-compatible 企业模型网关，以及路径/API/Schema 集成冲突分析。
- Vue 3 / TypeScript / Vite 前端构建骨架。
- 面向八类角色的治理工作台，可切换查看角色职责与工作重点。
- 项目组合指标、真实后端工作包、质量门禁和 Shared Context 事件视图。
- Vue 3 + Spring Boot/H2 持久化文章 CRUD 迁移切片、OpenAPI 契约和启动时执行的非破坏性 SQL。
- 项目级一次性 Windows 关机脚本；不监听可伪造文件，只有精确口令“做完任务关机”、验证完成、已推送、工作树干净且 `HEAD` 与 upstream 同步时才允许触发。
- 6 个前端契约测试，以及类型检查、生产构建和浏览器交互验收。
- 样例项目来源清单与固定版本工作区；当前 HEAD 为
  `5e62608af333b5757bf767e05b19e2b0d56f0afb`。

生产化扩展（不属于单节点 v0.1 MVP）：

- 企业 OIDC/LDAP 与组织级项目授权管理。
- H2 替换为受管数据库，进程内租约替换为多节点条件租约。
- 企业 CI/CD、代码托管和集中审计系统的生产连接器。

## 下一实现顺序

1. 在目标内网环境接入 OIDC/LDAP、TLS 和受管数据库。
2. 将企业模型网关地址、模型和密钥改为密钥管理服务注入。
3. 接入实际 Git 托管与 CI/CD，并执行组织级试点验收。

## 已知边界

- 当前主机具备 Java 21、Maven 3.9.2、Node 22 和 npm 10。
- 前端依赖已锁定在 `package-lock.json`；项目要求 Node 22.12 以上且低于 23。
- 样例项目使用 Java 7 时代工具链，后续需要独立 legacy runner，不能依赖控制平面的 Java 21 运行时直接启动。
- v0.1 是单项目、单节点参考实现；生产多节点部署需要外部一致性存储和分布式租约。
- Maven 首次执行需在项目内使用 `backend/.m2-repository`；PowerShell 调用批处理入口时应使用停止解析标记确保 `-D` 参数不丢失。
- 父工作区存在与本项目无关的用户改动；不得修改或清理。
